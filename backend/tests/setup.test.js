// Placeholder test to confirm Jest + ES modules are wired up correctly.
// Real tests (planner, stateManager, builder) are added in Phase 2.
test("jest is configured", () => {
  expect(1 + 1).toBe(2);
});
